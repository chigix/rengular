import { ShotScopeDirective } from './shot-scope.directive';

describe('ShotScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShotScopeDirective();
    expect(directive).toBeTruthy();
  });
});
