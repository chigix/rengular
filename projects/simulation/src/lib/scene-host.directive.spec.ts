import { SceneHostDirective } from './scene-host.directive';

describe('SceneHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SceneHostDirective();
    expect(directive).toBeTruthy();
  });
});
